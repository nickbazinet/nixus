// Story 4 (web-marketing-polish): Hero copy moved into i18n locale files
// (apps/web/src/locales/{en,fr}.json under `hero.*` keys). This module is
// kept for future layout-only constants — currently empty.
export {};
