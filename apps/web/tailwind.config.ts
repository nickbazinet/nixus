// Tailwind v4 is CSS-first. The canonical theme/tokens live in
// `packages/shared/src/styles/tokens.css` and are imported by
// `src/styles/main.css`. This file exists only to document that decision —
// no JS-side configuration is required for v4 + the `@tailwindcss/vite`
// plugin in this project.
//
// To customize tokens marketing-site-wide, extend them inside
// `src/styles/main.css` after the `@import '@nixus/shared/styles/tokens.css'`.
export default {};
