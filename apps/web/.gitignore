node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.local
# But keep the example committed
!.env.example
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Generated by TanStack Router (regenerated at build time via `pnpm generate`)
src/routeTree.gen.ts

# Playwright
test-results
playwright-report
playwright/.cache
