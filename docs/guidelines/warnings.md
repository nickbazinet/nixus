# Compilation Warnings

All compilation warnings must be resolved. 

If the warning is related to a "dead code", you should analyze if this method is really not used, and if it is not, you should remove it.
If the method is used, you should add an ignore for this warning.
